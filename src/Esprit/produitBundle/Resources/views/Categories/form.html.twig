<div class="well">
    <form method="POST" {{ form_enctype(form) }}>
        {{form_widget(form) }}
        <input type="submit" class="btn btn-primary" />
        <a href="{{path('produit_categories_index')}}" class="btn btn-warning">anuler</a>
    </form>
</div>